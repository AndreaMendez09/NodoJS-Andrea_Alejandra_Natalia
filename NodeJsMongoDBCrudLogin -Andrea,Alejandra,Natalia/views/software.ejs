<% include partials/_header %>

<head>
  <meta charset="utf-8">
  <title>Software</title>
  <!-- BOOTSTRAP CDN -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css"
    integrity="sha384-9gVQ4dYFwwWSjIDZnLEWnxCjeSWFphJiwGPXr1jddIhOegiu1FwO5qRGvFXOdJZ4" crossorigin="anonymous">
</head>
<div class="container">
  <%if(user.rol == "Administrador"){%>
  <div class="row">
    <div class="col-md-10">
      <div class="card">
        <div class="card-body">
          <form action="/software/add/<%= asignatura._id %>" method="post">
            <div class="form-group">
              <input class="form-control" name="description" cols="80"
                placeholder="Escribe la descripción del software"required>
            </div>
            <div class="form-group">
              <input class="form-control" name="url" cols="80"
                placeholder="Escribe la url" required></textarea>
            </div>
          
            <button class="btn btn-primary" type="submit">Añadir</button>
          </form>
          <form action='/software/archivos/<%= asignatura._id %>' method="POST" enctype="multipart/form-data">
            <input type ="file" name="file">
            <input type ="submit" value="Subir">
        </form>
        </div>
      </div>
      <br>
      <br>
    </div>
  </div>
  <% } %>

<div class="col-md-15">
  <table class="table table-bordered table-hover">
    <thead>
      <tr>
        <th>N°</th>
        <th>Descripcion</th>
        <th>Link</th>
        <th>Opciones</th>
      </tr>
    </thead>
    <tbody>
      <% for(var i=0; i < software.length; i++) { %>
        <tr>
            <td><%= i + 1%></td>
            <td><%= software[i].description%></td>
            <td>
              <%if( software[i].url!=""){%>
                <a href="../files/<%= software[i].url%>" > <%= software[i].url%></a>

                <% } %>  
                <%if( software[i].archivo!=""){%>
                 <a href="../files/<%= software[i].archivo%>" download="<%=software[i].archivo%>"> <%= software[i].archivo%></a>
  
                  <% } %>  
                 
            </td>
            <td>
              <a id="eliminar" onclick="pop_up()" href="/software/delete/<%= asignatura._id %>/<%= software[i]._id %>" class="btn btn-danger" >Delete</a>
            </td>
      </tr>
    <% } %>  
    </tbody>
  </table>
</div>
<script>
  function pop_up() {
    var elemento = document.getElementById("eliminar");
    if (confirm("¿Estas seguro de que quieres eliminar?")) {
      var hola;
    } else {
      elemento.setAttribute("href","/usuarios");
    }
  }
  </script>

<% include partials/_footer %>